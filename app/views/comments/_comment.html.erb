<li id= "comment-<%= comment.id %>"><%= comment.content %> by <%= comment.user.display_name %>

  <% if comment.user == current_user %>
    <%# TODO: 修改成 AJAX 版本的刪除 %>
    (<%= link_to "Delete", message_comment_path(@message, comment), :method => :delete, :remote => true %>)
  <% end %>
</li>
