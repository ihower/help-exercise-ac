<%if current_user.liked_message?(@message)%>
<%= button_to "Unlike", like_message_path(@message), :remote => true %>
<%else%>
<%= button_to "Like", like_message_path(@message), :remote => true %>
<%end%>